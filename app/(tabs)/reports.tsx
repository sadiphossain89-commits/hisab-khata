import { Feather } from '@expo/vector-icons'; import React from 'react'; import { StyleSheet, Text, View } from 'react-native'; import { useColors } from '@/hooks/useColors'; import { useKhata } from '@/context/KhataContext';
export default function Reports() { const c = useColors(); const { totalReceivable, totalPayable, totalExpenses, netProfit } = useKhata(); const rows = [['মোট পাওনা', totalReceivable, c.primary], ['মোট দেনা', totalPayable, '#477A5A'], ['মোট খরচ', totalExpenses, '#B35B56'], ['নিট ফলাফল', netProfit, netProfit >= 0 ? c.primary : '#B35B56']] as [string, number, string][]; return <View style={[s.root, { backgroundColor: c.background }]}><Text style={[s.title, { color: c.foreground }]}>রিপোর্ট</Text><Text style={{ color: c.mutedForeground }}>আপনার ব্যবসার আর্থিক সারাংশ</Text><View style={[s.card, { backgroundColor: c.card, borderColor: c.border }]}><View style={[s.reportIcon, { backgroundColor: c.secondary }]}><Feather name="bar-chart-2" size={22} color={c.secondaryForeground} /></View><Text style={[s.cardTitle, { color: c.foreground }]}>লাভ-ক্ষতির সামগ্রিক বিবরণী</Text>{rows.map(([label, value, color]) => <View key={label} style={[s.line, { borderBottomColor: c.border }]}><Text style={{ color: c.mutedForeground }}>{label}</Text><Text style={{ color, fontWeight: '800' }}>৳ {value.toLocaleString('bn-BD')}</Text></View>)}</View><View style={[s.tip, { backgroundColor: c.accent }]}><Feather name="info" size={18} color={c.accentForeground} /><Text style={{ flex: 1, color: c.accentForeground, lineHeight: 21 }}>নিয়মিত হিসাব রাখলে মাস শেষে আপনার ব্যবসার আসল চিত্র বুঝতে আরও সহজ হবে।</Text></View></View>; }
const s = StyleSheet.create({ root: { flex: 1, padding: 20, paddingTop: 56 }, title: { fontSize: 28, fontWeight: '800' }, card: { borderWidth: 1, borderRadius: 22, padding: 18, marginTop: 24 }, reportIcon: { width: 44, height: 44, borderRadius: 14, alignItems: 'center', justifyContent: 'center', marginBottom: 12 }, cardTitle: { fontSize: 18, fontWeight: '800', marginBottom: 14 }, line: { flexDirection: 'row', justifyContent: 'space-between', paddingVertical: 15, borderBottomWidth: 1 }, tip: { flexDirection: 'row', gap: 10, padding: 16, borderRadius: 17, marginTop: 14 } });